import 'package:flutter/material.dart';

void main() {
  //The runApp() function takes the given Widget and makes it the root of the widget tree.
  runApp(
    const Center(child: Text('Hello Text',textDirection: TextDirection.ltr),
    ),
  );
}

